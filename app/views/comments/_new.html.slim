= simple_form_for :comment, url: post_comments_path(post), method: :post  do |f|
  = f.input :comment, as: :text, placeholder: "Tulis komentar kamu disini...", input_html: { class: 'form-control' }
  
  = f.submit "Submit", class:"btn3d  btn btn-primary btn-md"
