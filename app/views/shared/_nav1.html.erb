<nav role="navigation" class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" data-target="#navbarCollapse" data-toggle="collapse" class="navbar-toggle">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <div class="navbar-brand"><%= link_to "GRUP JALAN", root_path %></div>
        </div>
        <!-- Collection of nav links and other content for toggling -->
        <div id="navbarCollapse" class="collapse navbar-collapse">
            <% if user_signed_in? %>
              <ul class="nav navbar-nav">
                  <li class="active"><%= link_to "Listing Post", index_path %></li>
                 <!--  <li><%= link_to "Profile", index_path %></li>
                  <li><%= link_to "Messages", index_path %></li> -->
              </ul>
              <ul class="nav navbar-nav navbar-right">
                  <li><%=link_to "Sign Out", destroy_user_session_path, method: :delete %></li>
                  <% if current_user.photo.url.nil? %>
                    <li><%=image_tag("blank-profile.png", size: "50x50", class: "img-circle")%></li>
                  <% else %>
                    <li><%=image_tag(current_user.photo.url, size: "50x50", class: "img-circle") %></li>
                  <% end %>  
              </ul>    
            <% else %>
              <ul class="nav navbar-nav navbar-right">
                    <li><%= link_to "Sign In", new_user_session_path %></li>
                    <li><%= link_to "Sign Up", new_user_registration_path %></li>
              </ul> 
            <% end %>    
        </div>
    </div>
</nav>